namespace Rystem.Authentication.Social
{
    public static class RystemClaimTypes
    {
        /// <summary>
        /// Defines a constant string representing a URL for language claims. It is used for identifying
        /// language-related information. For instance (en, it, es, etc).
        /// </summary>
        public const string Language = "http://schemas.rystem.org/claims/language";
    }
}
