import React from "react";

export const SocialLoginContextUpdate = React.createContext<React.DispatchWithoutAction>(() => { });
export const SocialLoginContextRefresh = React.createContext<React.DispatchWithoutAction>(() => { });
export const SocialLoginContextLogout = React.createContext<React.DispatchWithoutAction>(() => { });
